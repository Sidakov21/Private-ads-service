# Сервис частных объявлений (УП-2 по ТРПО)
## Сидаков Амир и Али

## 📝 Краткое описание проекта

**"Сервис частных объявлений"** — это настольное приложение для управления объявлениями, разработанное в рамках практического задания по Технологиям разработки программного обеспечения.

Приложение реализовано на платформе **WPF (.NET Framework)** и использует работу с моделью **ADO.NET** для взаимодействия с централизованной базой данных **MS SQL Server**. Система поддерживает авторизацию пользователей и включает ключевой функционал для управления объявлениями, а также механизм расчета прибыли.

---

## 🌟 Основные возможности системы

| Функционал | Описание | Связанные модули |
| :--- | :--- | :--- |
| **Просмотр и Фильтрация** | Каталог объявлений доступен неавторизованным пользователям. Реализована расширенная фильтрация по городу, категории, типу и статусу. | `MainWindow.xaml.cs` |
| **Авторизация/Сессии** | Управление доступом пользователей через менеджер сессий (`SessionManager`). Разделение функционала для авторизованных и неавторизованных пользователей. | `AuthorizationWindow.xaml.cs` |
| **Управление объявлениями** | Авторизованные пользователи могут добавлять, редактировать и удалять только свои объявления. | `AdEditorWindow.xaml.cs` |
| **Расчет прибыли (Монетизация)** | При переводе объявления в статус **"Завершено"** система автоматически запрашивает полученную сумму и обновляет общее поле `profit` пользователя в таблице `Users`. | `AdEditorWindow.xaml.cs` |
| **Валидация данных** | Строгая проверка на заполнение обязательных полей (`Заголовок`, `Цена`) перед сохранением в БД. | `AdEditorWindow.xaml.cs` |
| **Работа с изображениями** | Загрузка и копирование выбранных изображений в локальную папку проекта (`/ФотоКарточек`) с сохранением относительного пути в БД. | `AdEditorWindow.xaml.cs` |

---

## 🛠️ Технологический стек

* **Платформа:** WPF (.NET Framework 4.7.2)
* **Язык программирования:** C#
* **База данных:** MS SQL Server
* **Моделирование:** PlantUML (для диаграмм прецедентов и классов)
* **Тестирование:** Ручное тестирование (по разработанным тестовым сценариям)

---

## ⚙️ Инструкции по запуску

Для успешного запуска проекта необходимо выполнить следующие шаги:

### 1. Подготовка базы данных (SQL Server)

Убедитесь, что на вашем сервере MS SQL Server развернута база данных **`Sidakov_DB_PrivateAds_TEST`**.

### 2. Настройка строки подключения

В файле `App.config` необходимо обновить строку подключения для `Sidakov_DB_PrivateAdsEntities_TEST`, указав корректный `data source` (имя вашего SQL сервера).

### 3. Компиляция и запуск
Откройте решение в `Visual Studio`.

Выполните `Build Solution` (Сборка решения).

Запустите приложение (F5 или кнопка Start).

## 🛡️ Тестирование и стабильность
В ходе разработки были устранены все критические ошибки, включая проблему с нарушением первичного ключа при добавлении новых записей в БД.

Проект прошел модульное тестирование ключевых бизнес-сервисов (валидация, расчет прибыли) и ручное тестирование пользовательского интерфейса, подтвердив стабильность и соответствие всем функциональным требованиям.

